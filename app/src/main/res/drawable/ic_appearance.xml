<?xml version="1.0" encoding="utf-8"?>
<!--
  A capital "T", the standard glyph for a title/typography control (Material's own "title"
  icon). Kept as a single flat path since this is a 20dp section marker, not artwork, and the
  fill is the same brand green as ic_bignum's accent so all three section icons read as one set.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="#10B981"
        android:pathData="M5,4v3h5.5v12h3V7H19V4H5z" />
</vector>
